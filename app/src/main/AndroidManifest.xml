<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lookingforthecost">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_ALARM" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OptionActivity"></activity>
        <activity android:name=".Output" />
        <activity android:name=".screens.plan.create_plan.CreatePlan" />
        <activity android:name=".screens.plan.MyPlan" />
        <activity android:name=".screens.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screens.spending.create_caterory_and_spending.CreateSpendingActivity" />
        <activity android:name=".screens.spending.create_caterory_and_spending.CreateCategoryActivity" />
        <activity android:name=".screens.spending.SpendingActivity" />

        <receiver android:name=".notification.NotificationMessage" />
    </application>

</manifest>